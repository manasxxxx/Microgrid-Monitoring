# Example environment variables for development
# Create a file named .env in the project root and set your real values there.

# ThingSpeak read API key (optional). If set, it will be used instead of the UI-provided key.
# Vite exposes env vars starting with VITE_ to the client bundle.
VITE_THINGSPEAK_API_KEY= MJRHL0N7RNVBSM9H
